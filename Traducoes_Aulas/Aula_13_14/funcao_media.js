// Exemplo de função que calcula a média de duas notas e retorna o resultado.

function media(p1, p2) {
    const calculo = (p1 + p2) / 2;
    return calculo;
}

// Programa principal
let usarDepois = media(9, 8);
console.log("Média é:", usarDepois);
